<%= form_for(@industry_category) do |f| %>
  <% if @industry_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@industry_category.errors.count, "error") %> prohibited this industry_category from being saved:</h2>

      <ul>
      <% @industry_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :parent_id %><br>
    <%= f.number_field :parent_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
