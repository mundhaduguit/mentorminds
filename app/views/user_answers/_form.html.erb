<%= form_for(@user_answer) do |f| %>
  <% if @user_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_answer.errors.count, "error") %> prohibited this user_answer from being saved:</h2>

      <ul>
      <% @user_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
  </div>
  
<div style="display:none"> <!--Security flaw need to checked -->
<%#TODO %>
  <%= f.text_field :status ,:value => 'in progress', :hidden => true %>
  <%= f.number_field :user_id ,:value => current_user.id, :hidden => true%>
    <% user_challenge_id =@user_challenge.present? ? @user_challenge.id : params[:user_challenge_id] %>
    <%= f.number_field :user_challenge_id, :value => user_challenge_id , :hidden => true%>
</div>
  
    
  <div class="actions">
    <%= f.submit "Answer"%>
  </div>
<% end %>
