<%= form_for(@industry) do |f| %>
  <% if @industry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@industry.errors.count, "error") %> prohibited this industry from being saved:</h2>

      <ul>
      <% @industry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-3">
      <div class="multi-col-form">
        <div class="form-group col-md-12">
          <%= f.label :name %><br>
          <%= f.text_field :name, class: 'form-control '%>
        </div>
        <div class="form-group col-md-12">
          <%= f.label :description %><br>
          <%= f.text_field :description, class: 'form-control ' %>
        </div>
        <div class="form-group col-md-12">
          <%= f.label :industry_category_id %><br>
          <%= f.number_field :industry_category_id, class: 'form-control ' %>
        </div>
          <div class="form-group col-md-12">
        	 <%= f.label :image %><br>
        	 <%= f.file_field :image %>
        </div>
       <div class="actions">
        <%= f.submit :class=>"btn btn-primary" %>
      </div>
    </div>
  </div>
<% end %>
